// Private podcast feed token. Stable across builds: the user's podcast app
// subscribes to a URL containing it, so never rotate without notice.
export const feedToken = 'b8fbf8deafd2c418c0ccb59e';
export const feedTitle = 'Agentic Engineering — Audio Edition';
